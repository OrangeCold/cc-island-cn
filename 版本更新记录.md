# 版本更新记录（cc-island-cn）

本文件记录**上游同步基线**、**日常选择性同步 bug fix 的操作流程**，以及**历次同步记录**。

> cc-island-cn 基于 vibe-notch（Apache-2.0）二次开发，采用「断开 fork、保留 upstream」的方式独立维护：新功能按自身规划开发，仅选择性同步上游的 bug fix。

---

## 一、当前同步基线

> 每次成功同步并合入 main 后，更新本节为新的上游点。

| 项目 | 值 |
|------|-----|
| 上游仓库 | vibe-notch |
| 上游地址 | https://github.com/farouqaldori/vibe-notch |
| 本地 origin | git@github.com:OrangeCold/cc-island-cn.git |
| 当前对齐 commit | `10f1d24`（Drop sub tag from README heading）|
| 版本描述 | `v1.3.1-2-g10f1d24`（即 v1.3.1 之后第 2 个提交）|
| 起始基线时间 | 2026-06-18 |

---

## 二、日常操作：选择性同步上游 bug fix

### 核心原则

- **只 cherry-pick 上游的 bug fix（`fix:`），不合并新功能（`feat:`）**
- 新功能按 cc-island-cn 自身规划独立开发，避免被上游功能节奏"绑架"
- 始终保留 `upstream` 渠道：`origin` = 自己的仓库，`upstream` = vibe-notch

### 标准步骤

```bash
# 1. 拉取上游最新代码与 tag
git fetch upstream --tags

# 2. 在独立分支上操作，不直接污染 main（日期写死，便于追溯）
git checkout -b sync/upstream-2026-06-18

# 3. 查看自上次基线以来上游新增的所有 commit（用上一次的基线 tag/commit 替换 v1.3.1）
git log v1.3.1..upstream/main --oneline

# 4. 对照上游官方 Release Notes，挑出 fix: 开头的 commit
#    feat: / docs: / chore: 等通常跳过

# 5. 逐个 cherry-pick 选中的 bug fix
git cherry-pick <commit-1> <commit-2>

# 6. 若冲突：解决后继续
#    git status 查看冲突文件 → 手动修改 → 标记已解决
git add .
git cherry-pick --continue

# 7. 本地 Xcode 编译 + 运行验证通过后，合回 main
git checkout main
git merge sync/upstream-2026-06-18

# 8. 更新本文件「当前同步基线」为本次同步到的上游 commit/tag
# 9. 在「同步记录」表追加一行

# 10. 推送到自己的仓库
git push origin main
```

### 判断某个 commit 是否值得 cherry-pick

逐个评估：

1. **看类型**：`fix:` 优先，`feat:` / `refactor:` 谨慎，`docs:` / `chore:` 一般跳过
2. **看范围**：改动越小、越聚焦的越适合；大范围重构改动容易冲突
3. **看适用性**：若 bug 出现在已被本方大改的代码里，可能 patch 不上 → 先判断"这个 bug 在 cc-island-cn 里还存在吗"
4. **看冲突**：冲突过大说明与本地改动强耦合 → 放弃 cherry-pick，改为手动移植逻辑

### 辅助命令

```bash
# 快速浏览上游最近 20 个提交
git fetch upstream --tags 2>/dev/null && git log upstream/main --oneline -20

# 查看某个 commit 的完整改动，决定要不要合
git show <commit>

# 撤销一次进行中/失败的 cherry-pick
git cherry-pick --abort

# 查看当前基线相对上游落后多少
git fetch upstream --tags
git log HEAD..upstream/main --oneline
```

---

## 三、同步记录

> 每次同步后在此追加一行，便于追溯与避免重复 cherry-pick。

| 日期 | 同步到的上游点 | 同步内容 | cherry-pick 的 commit | 备注 |
|------|--------------|----------|----------------------|------|
| 2026-06-18 | `10f1d24`（v1.3.1-2） | 初始基线，clone 自上游 main | — | 二开起点，尚未同步任何上游更新 |

---

## 四、维护说明

- **基线更新时机**：每次成功 cherry-pick 并合入 main 后，把「当前同步基线」更新为本次同步到的上游 commit/tag。
- **避免重复 cherry-pick**：建议在 commit message 中带上游标记，如 `fix: [upstream abc1234] 修复内存泄漏`，便于后续查重。
- **批量同步（慎用）**：仅当上游某次发布 bug fix 集中、且与本地无冲突时，才考虑 `git merge upstream/main` 后再 revert 不想要的新功能——通常比逐个 cherry-pick 更累，优先用 cherry-pick。
- **改动隔离原则**：本方新功能尽量放独立文件/模块，对上游文件的改动尽量小，可大幅降低 cherry-pick 冲突概率。
