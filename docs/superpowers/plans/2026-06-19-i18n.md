# cc-island-cn 国际化（中英双语 + 设置页语言切换）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户在灵动岛设置菜单切换语言（跟随系统 / 简体中文 / English），并将全部用户可见文案本地化为中英双语。

**Architecture:** Xcode String Catalog（`Localizable.xcstrings`，源语言 `en`）作本地化底座；`AppLanguage` 枚举封装语言选择与 `AppleLanguages` 写入；偏好走现有 `AppSettings` + `UserDefaults`；切换后写 `AppleLanguages` 并 relaunch 生效（macOS 运行时切换需重启）。

**Tech Stack:** Swift 5 / SwiftUI / macOS 15.6+ / Xcode 16+（project object version 77，fileSystemSynchronizedRootGroup）/ String Catalog。

## Global Constraints

- **部署目标**：macOS 15.6（`MACOSX_DEPLOYMENT_TARGET = 15.6`）。
- **项目格式**：Xcode project object version 77，`CcIslandCn` 目录是 `PBXFileSystemSynchronizedRootGroup`。**新增 `.swift` / `.xcstrings` 放进 `CcIslandCn/` 子目录即自动纳入 target，无需手工编辑 pbxproj 添加文件引用。** 唯一需改 pbxproj 的是 `knownRegions`（Task 2）。
- **无测试 target**：仓库无 `Tests/`。本计划遵循 spec 决策——**不新建 test target**（纯逻辑极薄，单测性价比低）。验证以 `xcodebuild` 构建成功 + 手动清单为主。`AppLanguage` 的正确性由类型系统 + 手动清单第 7 项（旧用户升级回退）保证。
- **默认语言**：跟随系统（`.system`，不写 `AppleLanguages`）；开发/源语言 `en`。
- **key 命名**：直接用现有英文字面量作 String Catalog key，不规范化。
- **语言交互**：所有交互、注释、commit message 用简体中文；技术术语保留英文。

---

## File Structure

| 文件 | 动作 | 职责 |
|------|------|------|
| `CcIslandCn/Core/AppLanguage.swift` | 新增 | 语言枚举（`system`/`zhHans`/`en`）+ `apply()`（写 `AppleLanguages`）+ `relaunch(completion:)` |
| `CcIslandCn/Core/Settings.swift` | 修改 | 新增 `appLanguage` key 与计算属性（未知值回退 `.system`） |
| `CcIslandCn/Resources/Localizable.xcstrings` | 新增 | String Catalog，源语言 `en`，含 `zh-Hans` |
| `CcIslandCn/UI/Components/LanguagePickerRow.swift` | 新增 | 语言选择行（仿 `ClaudeDirPickerRow`），选中触发 apply + relaunch alert |
| `CcIslandCn/UI/Views/NotchMenuView.swift` | 修改 | 加 `LanguagePickerRow()` 一行 |
| `CcIslandCn.xcodeproj/project.pbxproj` | 修改 | `knownRegions` 追加 `zh-Hans` |
| `CcIslandCn/Info.plist` | 修改 | 加 `CFBundleLocalizations` |
| 其余 UI 文件 | 仅翻译 | `ChatView` / `ToolResultViews` / `ClaudeInstancesView` / `ScreenPickerRow` / `SoundPickerRow` / `ClaudeDirPickerRow`：逻辑不改，仅 String Catalog 补 zh-Hans |

---

## Task 1: AppLanguage 模块 + AppSettings.appLanguage

**Files:**
- Create: `CcIslandCn/Core/AppLanguage.swift`
- Modify: `CcIslandCn/Core/Settings.swift`（`Keys` 段 + enum 末尾新增计算属性）

**Interfaces:**
- Produces:
  - `enum AppLanguage: String, CaseIterable` — cases: `system`（rawValue `"system"`）、`zhHans`（rawValue `"zh-Hans"`）、`en`（rawValue `"en"`）
  - `func AppLanguage.apply()` — 写/清 `UserDefaults` 的 `AppleLanguages`
  - `static func AppLanguage.relaunch(completion: @escaping (Bool) -> Void)` — 重启自身
  - `static var AppSettings.appLanguage: AppLanguage` — get/set，未知值回退 `.system`

- [ ] **Step 1: 创建 `CcIslandCn/Core/AppLanguage.swift`**

```swift
//
//  AppLanguage.swift
//  CcIslandCn
//
//  用户语言选择：跟随系统 / 简体中文 / English。
//  apply() 写 AppleLanguages；切换需重启 App 才生效，由 relaunch() 完成。
//

import AppKit
import Foundation

/// 用户可选的语言。`.system` 表示跟随系统语言（不写 AppleLanguages）。
enum AppLanguage: String, CaseIterable {
    case system
    case zhHans = "zh-Hans"
    case en

    /// 按选择写或清 AppleLanguages。需重启 App 后由 Bundle 重新读取生效。
    func apply() {
        let defaults = UserDefaults.standard
        switch self {
        case .system:
            defaults.removeObject(forKey: "AppleLanguages")
        case .zhHans:
            defaults.set(["zh-Hans"], forKey: "AppleLanguages")
        case .en:
            defaults.set(["en"], forKey: "AppleLanguages")
        }
    }

    /// 重启自身。成功启动新实例后终止当前进程并回调 true；失败回调 false（调用方提示手动重启）。
    static func relaunch(completion: @escaping (Bool) -> Void) {
        let bundleURL = URL(fileURLWithPath: Bundle.main.bundlePath)
        let configuration = NSWorkspace.OpenConfiguration()
        NSWorkspace.shared.openApplication(at: bundleURL, configuration: configuration) { _, error in
            if let error {
                print("Relaunch failed: \(error)")
                completion(false)
            } else {
                DispatchQueue.main.async { NSApp.terminate(nil) }
                completion(true)
            }
        }
    }
}
```

- [ ] **Step 2: 修改 `CcIslandCn/Core/Settings.swift`**

在 `Keys` enum 内追加 `appLanguage`（第 39–42 行区域）：

```swift
    private enum Keys {
        static let notificationSound = "notificationSound"
        static let claudeDirectoryName = "claudeDirectoryName"
        static let appLanguage = "appLanguage"
    }
```

在 `enum AppSettings` 末尾（`claudeDirectoryName` 计算属性之后、右花括号之前）新增：

```swift
    // MARK: - App Language

    /// 用户语言选择。未知值（旧版无 key / 手改 plist 的非法值）回退 `.system`（跟随系统）。
    static var appLanguage: AppLanguage {
        get {
            guard let rawValue = defaults.string(forKey: Keys.appLanguage) else {
                return .system
            }
            return AppLanguage(rawValue: rawValue) ?? .system
        }
        set {
            defaults.set(newValue.rawValue, forKey: Keys.appLanguage)
        }
    }
```

- [ ] **Step 3: 构建验证**

Run:
```bash
xcodebuild -scheme CcIslandCn -configuration Debug \
  -destination 'generic/platform=macOS' build 2>&1 | tail -5
```
Expected: `** BUILD SUCCEEDED **`（或最后一行为 `BUILD SUCCEEDED`）。

- [ ] **Step 4: 提交**

```bash
git add CcIslandCn/Core/AppLanguage.swift CcIslandCn/Core/Settings.swift
git commit -m "feat: 新增 AppLanguage 语言枚举与 appLanguage 偏好"
```

---

## Task 2: 本地化基础设施（String Catalog + knownRegions + Info.plist）

**Files:**
- Create: `CcIslandCn/Resources/Localizable.xcstrings`
- Modify: `CcIslandCn.xcodeproj/project.pbxproj`（`knownRegions` 段，第 113–116 行）
- Modify: `CcIslandCn/Info.plist`（新增 `CFBundleLocalizations`）

**Interfaces:**
- Produces: 一个可工作的本地化骨架——源语言 `en`、支持 `zh-Hans` 区域、String Catalog 已纳入 target。后续任务的翻译都写入此文件。

- [ ] **Step 1: 创建 `CcIslandCn/Resources/Localizable.xcstrings`**

写入以下 JSON（空 strings，build 后 Xcode 会自动从代码抽取 `LocalizedStringKey` 填充）：

```json
{
  "sourceLanguage" : "en",
  "strings" : {

  },
  "version" : "1.0"
}
```

- [ ] **Step 2: 修改 `CcIslandCn.xcodeproj/project.pbxproj` 的 `knownRegions`**

将第 113–116 行：
```
			knownRegions = (
				en,
				Base,
			);
```
改为：
```
			knownRegions = (
				en,
				Base,
				zh-Hans,
			);
```

- [ ] **Step 3: 修改 `CcIslandCn/Info.plist`**

在 `<dict>` 内（建议紧跟 `LSUIElement` 的 `</true>` 之后，第 16 行后）新增：

```xml
	<key>CFBundleLocalizations</key>
	<array>
		<string>en</string>
		<string>zh-Hans</string>
	</array>
```

- [ ] **Step 4: 构建并确认 String Catalog 自动抽取**

Run:
```bash
xcodebuild -scheme CcIslandCn -configuration Debug \
  -destination 'generic/platform=macOS' build 2>&1 | tail -5
```
Expected: `** BUILD SUCCEEDED **`。

构建后检查 xcstrings 是否被自动填入 key（Xcode 在 build 阶段抽取 `LocalizedStringKey`）：
```bash
python3 -c "import json;d=json.load(open('CcIslandCn/Resources/Localizable.xcstrings'));print('keys:',len(d['strings']));print(list(d['strings'].keys())[:10])"
```
Expected: `keys:` 一个 > 0 的数（例如已含 `"Back"`、`"Launch at Login"`、`"Quit"` 等），打印前 10 个 key。

> **若 keys 为 0**：用 Xcode 打开项目，双击 `Localizable.xcstrings` 进入 String Catalog 编辑器，Xcode 会触发抽取并回填；保存后再跑上面的检查命令。

- [ ] **Step 5: 提交**

```bash
git add CcIslandCn/Resources/Localizable.xcstrings CcIslandCn.xcodeproj/project.pbxproj CcIslandCn/Info.plist
git commit -m "feat: 搭建本地化骨架（String Catalog + zh-Hans 区域）"
```

---

## Task 3: LanguagePickerRow + 接入菜单 + relaunch

**Files:**
- Create: `CcIslandCn/UI/Components/LanguagePickerRow.swift`
- Modify: `CcIslandCn/UI/Views/NotchMenuView.swift`（第 44 行 `ClaudeDirPickerRow()` 之后插入一行）

**Interfaces:**
- Consumes: `AppSettings.appLanguage`、`AppLanguage.apply()`、`AppLanguage.relaunch(completion:)`（均来自 Task 1）
- Produces: `struct LanguagePickerRow: View`

- [ ] **Step 1: 创建 `CcIslandCn/UI/Components/LanguagePickerRow.swift`**

样式严格对齐现有 `ClaudeDirPickerRow`（主行 + 内联展开选项 + 选中圆点 + checkmark）。语言名按各自语言显示：`简体中文`/`English` 固定（用 `Text(verbatim:)`），仅 `Follow System` 走本地化。

```swift
//
//  LanguagePickerRow.swift
//  CcIslandCn
//
//  语言选择行：跟随系统 / 简体中文 / English。选中后写偏好并提示重启生效。
//

import SwiftUI

struct LanguagePickerRow: View {
    @State private var isExpanded: Bool = false
    @State private var isHovered: Bool = false
    @State private var selection: AppLanguage = AppSettings.appLanguage
    @State private var showRelaunchAlert: Bool = false

    var body: some View {
        VStack(spacing: 0) {
            Button {
                withAnimation(.easeInOut(duration: 0.2)) {
                    isExpanded.toggle()
                }
            } label: {
                HStack(spacing: 10) {
                    Image(systemName: "globe")
                        .font(.system(size: 12))
                        .foregroundColor(textColor)
                        .frame(width: 16)

                    Text("Language")
                        .font(.system(size: 13, weight: .medium))
                        .foregroundColor(textColor)

                    Spacer()

                    displayNameView(selection)
                        .font(.system(size: 11))
                        .foregroundColor(.white.opacity(0.4))
                        .lineLimit(1)

                    Image(systemName: isExpanded ? "chevron.up" : "chevron.down")
                        .font(.system(size: 10))
                        .foregroundColor(.white.opacity(0.4))
                }
                .padding(.horizontal, 12)
                .padding(.vertical, 10)
                .background(
                    RoundedRectangle(cornerRadius: 8)
                        .fill(isHovered ? Color.white.opacity(0.08) : Color.clear)
                )
            }
            .buttonStyle(.plain)
            .onHover { isHovered = $0 }

            if isExpanded {
                VStack(spacing: 2) {
                    ForEach(AppLanguage.allCases, id: \.self) { language in
                        LanguageOptionRow(
                            language: language,
                            isSelected: selection == language
                        ) {
                            choose(language)
                        }
                    }
                }
                .padding(.leading, 28)
                .padding(.top, 4)
            }
        }
        .onAppear { selection = AppSettings.appLanguage }
        .alert("Language changes take effect after restart", isPresented: $showRelaunchAlert) {
            Button("Restart Now") {
                AppLanguage.relaunch { ok in
                    if !ok {
                        print("Relaunch failed — user should restart manually")
                    }
                }
            }
            Button("Later", role: .cancel) {}
        } message: {
            Text("Restart the app to apply the new language.")
        }
    }

    private var textColor: Color {
        .white.opacity(isHovered ? 1.0 : 0.7)
    }

    private func choose(_ language: AppLanguage) {
        selection = language
        AppSettings.appLanguage = language
        language.apply()
        isExpanded = false
        showRelaunchAlert = true
    }

    /// 语言名：Follow System 走本地化；简体中文 / English 固定显示（语言名用各自语言是语言选择器惯例）。
    @ViewBuilder
    private func displayNameView(_ language: AppLanguage) -> some View {
        switch language {
        case .system: Text("Follow System")
        case .zhHans: Text(verbatim: "简体中文")
        case .en: Text(verbatim: "English")
        }
    }
}

private struct LanguageOptionRow: View {
    let language: AppLanguage
    let isSelected: Bool
    let action: () -> Void

    @State private var isHovered = false

    var body: some View {
        Button(action: action) {
            HStack(spacing: 8) {
                Circle()
                    .fill(isSelected ? TerminalColors.green : Color.white.opacity(0.2))
                    .frame(width: 6, height: 6)

                displayName
                    .font(.system(size: 12, weight: .medium))
                    .foregroundColor(.white.opacity(isHovered ? 1.0 : 0.7))

                Spacer()

                if isSelected {
                    Image(systemName: "checkmark")
                        .font(.system(size: 10, weight: .bold))
                        .foregroundColor(TerminalColors.green)
                }
            }
            .padding(.horizontal, 10)
            .padding(.vertical, 6)
            .background(
                RoundedRectangle(cornerRadius: 6)
                    .fill(isHovered ? Color.white.opacity(0.06) : Color.clear)
            )
        }
        .buttonStyle(.plain)
        .onHover { isHovered = $0 }
    }

    @ViewBuilder
    private var displayName: some View {
        switch language {
        case .system: Text("Follow System")
        case .zhHans: Text(verbatim: "简体中文")
        case .en: Text(verbatim: "English")
        }
    }
}
```

- [ ] **Step 2: 修改 `CcIslandCn/UI/Views/NotchMenuView.swift` 接入新行**

将第 42–44 行：
```swift
                ScreenPickerRow(screenSelector: screenSelector)
                SoundPickerRow(soundSelector: soundSelector)
                ClaudeDirPickerRow()
```
改为：
```swift
                ScreenPickerRow(screenSelector: screenSelector)
                SoundPickerRow(soundSelector: soundSelector)
                ClaudeDirPickerRow()
                LanguagePickerRow()
```

- [ ] **Step 3: 构建验证**

Run:
```bash
xcodebuild -scheme CcIslandCn -configuration Debug \
  -destination 'generic/platform=macOS' build 2>&1 | tail -5
```
Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 4: 手动验证（语言切换管道）**

构建产物运行（`open` .app 或 Xcode Run），打开灵动岛设置菜单：
1. 「Appearance」段出现「Language」行，当前显示 `Follow System`（或 `English`）。
2. 点击展开，见三选项：Follow System / 简体中文 / English。
3. 选「简体中文」→ 弹 alert「Language changes take effect after restart」→ 点「Restart Now」→ App 重启。
4. 重启后回到菜单：此时文案**仍是英文**（翻译在 Task 4 填），但「Language」行右侧显示「简体中文」，证明 `AppleLanguages` 写入 + relaunch 管道工作。
5. 选「Follow System」→ 重启 → 行右侧回到 `Follow System`。
6. 选「English」→ alert「Later」→ 本次不重启；下次启动才生效（偏好已写）。

> 完整链路（菜单文案变中文）需 Task 4 完成翻译后再验。

- [ ] **Step 5: 提交**

```bash
git add CcIslandCn/UI/Components/LanguagePickerRow.swift CcIslandCn/UI/Views/NotchMenuView.swift
git commit -m "feat: 设置菜单新增语言选择行与重启生效交互"
```

---

## Task 4: 全量翻译（补 zh-Hans）

**Files:**
- Modify: `CcIslandCn/Resources/Localizable.xcstrings`（为每个 key 补 zh-Hans）

**说明 — xcstrings 每条 key 的 JSON 格式**：在 `"strings"` 对象内，每个 key 形如：
```json
"Back" : {
  "localizations" : {
    "zh-Hans" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "返回"
      }
    }
  }
}
```
**推荐执行方式**：用 Xcode 打开 `Localizable.xcstrings`，在 String Catalog 编辑器的 `zh-Hans` 列直接粘贴下表「中文译文」（Xcode 已自动抽取所有 key，无需手写 key）。**或**直接编辑 JSON，按上面对每个 key 补 `localizations`。

> 含插值的 key（表中 `%@` / `%lld`）：占位符类型由代码里插值变量类型决定（`Int`→`%lld`、`String`→`%@`）。Xcode 抽取的实际 key 会含正确占位符——以编辑器里看到的 key 为准，把下表中文译文里的占位符替换成实际 key 的占位符即可。

### Step 1 — 设置菜单与通用组件

对下表每个 key 填 zh-Hans（来源：`NotchMenuView`、`LanguagePickerRow`、`ScreenPickerRow`、`SoundPickerRow`、`ClaudeDirPickerRow`）：

| key（英文） | zh-Hans 译文 |
|---|---|
| Back | 返回 |
| Screen | 屏幕 |
| Notification Sound | 通知声音 |
| Claude Directory | Claude 目录 |
| Auto-detect | 自动检测 |
| Choose folder… | 选择文件夹… |
| Language | 语言 |
| Follow System | 跟随系统 |
| Launch at Login | 登录时启动 |
| Hooks | Hooks |
| Accessibility | 辅助功能 |
| On | 开 |
| Off | 关 |
| Enable | 开启 |
| Check for Updates | 检查更新 |
| Checking... | 检查中… |
| Download Update | 下载更新 |
| Downloading... | 下载中… |
| Extracting... | 解压中… |
| Install & Relaunch | 安装并重启 |
| Installing... | 安装中… |
| Update failed | 更新失败 |
| Up to date | 已是最新 |
| Retry | 重试 |
| Star on GitHub | 在 GitHub 加星 |
| Quit | 退出 |
| Restart Now | 立即重启 |
| Later | 稍后 |
| Language changes take effect after restart | 语言更改将在重启后生效 |
| Restart the app to apply the new language. | 重启应用以使新语言生效。 |

- [ ] 构建并提交：
```bash
xcodebuild -scheme CcIslandCn -configuration Debug -destination 'generic/platform=macOS' build 2>&1 | tail -3
git add CcIslandCn/Resources/Localizable.xcstrings
git commit -m "i18n: 设置菜单与通用组件 zh-Hans 翻译"
```

### Step 2 — ChatView + ClaudeInstancesView

固定文案：

| key | zh-Hans |
|---|---|
| Loading messages... | 正在加载消息… |
| No messages yet | 暂无消息 |
| Interrupted | 已中断 |
| Claude Code needs your input | Claude Code 需要你的输入 |
| Terminal | 终端 |
| Deny | 拒绝 |
| Allow | 允许 |
| No sessions | 暂无会话 |
| Run claude in terminal | 在终端运行 claude |
| Needs your input | 需要你的输入 |
| You: | 你： |

含插值（`ChatView`，占位符以实际抽取 key 为准）：

| 代码字面量 | zh-Hans 译文 |
|---|---|
| Image (\(image.mediaType)) | 图像（%@） |
| +\(hiddenCount) more tool uses | 另有 %@ 个工具调用 |
| Subagent used \(tools.count) tools: | 子代理使用了 %@ 个工具： |
| \(taskDesc) (\(tool.subagentTools.count) tools) | %@（%@ 个工具） |

> 上述含插值 key 的实际形态可能是 `Image (%@)`、`+%lld more tool uses`、`Subagent used %lld tools:`、`%@ (%lld tools)`——以 Xcode 抽取为准，逐一对齐占位符。

- [ ] 构建并提交：
```bash
xcodebuild -scheme CcIslandCn -configuration Debug -destination 'generic/platform=macOS' build 2>&1 | tail -3
git add CcIslandCn/Resources/Localizable.xcstrings
git commit -m "i18n: ChatView 与 ClaudeInstancesView zh-Hans 翻译"
```

### Step 3 — ToolResultViews

固定文案：

| key | zh-Hans |
|---|---|
| (User modified) | （用户已修改） |
| stderr: | 标准错误： |
| (No content) | （无内容） |
| No matches found | 未找到匹配项 |
| No files found | 未找到文件 |
| ... and more (truncated) | …更多（已截断） |
| No results found | 未找到结果 |
| Completed | 已完成 |

含插值（占位符以实际抽取 key 为准）：

| 代码字面量 | zh-Hans 译文 |
|---|---|
| \(result.numFiles) files with matches | %@ 个文件匹配 |
| \(tools) tools | %@ 个工具 |
| Status: \(result.status) | 状态：%@ |
| Exit: \(exitCode) | 退出码：%@ |
| Background task: \(bgId) | 后台任务：%@ |
| ... and \(result.results.count - 5) more results | …另有 %@ 条结果 |
| ... (\(lines.count - maxLines) more lines) | …（另有 %@ 行） |
| ... and \(files.count - limit) more files | …另有 %@ 个文件 |
| ... and \(patches.count - 3) more hunks | …另有 %@ 个差异段 |

> 仅 `Text("...")` / `Text("…")` 这类纯符号省略号**不翻译**（非语言内容）。`\(lineNumber)`、`\(result.code)`、`\(formatDuration(duration))` 等纯插值 `Text` **不翻译**。

- [ ] 构建并提交：
```bash
xcodebuild -scheme CcIslandCn -configuration Debug -destination 'generic/platform=macOS' build 2>&1 | tail -3
git add CcIslandCn/Resources/Localizable.xcstrings
git commit -m "i18n: ToolResultViews zh-Hans 翻译"
```

### Step 4 — 验证翻译完整性

- [ ] 用脚本列出所有 key，确认无遗漏的 zh-Hans：

```bash
python3 -c "
import json
d = json.load(open('CcIslandCn/Resources/Localizable.xcstrings'))
missing = [k for k,v in d['strings'].items() if 'zh-Hans' not in v.get('localizations', {})]
print('total keys:', len(d['strings']))
print('missing zh-Hans:', missing)
"
```
Expected: `missing zh-Hans: []`（或仅剩纯符号/纯插值 key，这类应从翻译中排除——若出现，确认其为非语言内容即可）。

- [ ] **手动验证（完整双语链路）**：

1. 系统中文 + `appLanguage = system`（删 `~/Library/Preferences/com.celestial.CcIslandCn.plist` 的 `appLanguage` 与 `AppleLanguages` 后启动）→ 菜单全中文。
2. 切「English」→ 重启 → 全英文。
3. 切「简体中文」→ 重启 → 全中文，重点抽查：
   - 设置菜单（Back / Launch at Login / Hooks / Accessibility / On / Off / Enable / Language 行）
   - `UpdateRow` 状态文案（Check for Updates / Up to date / Retry）
   - `ChatView`（Claude Code needs your input / Allow / Deny）
   - `ClaudeInstancesView`（No sessions / Go to Terminal）
   - `ToolResultViews`（No matches found / Completed / No results found）
4. 旧用户升级路径：无 `appLanguage` key → 默认 `.system`，无异常。
5. 完整链路验证需本机装有 Claude Code CLI 并有活跃会话（socket 事件 + JSONL）。

- [ ] **最终提交（若 Step 4 有补译/修正）**：
```bash
git add CcIslandCn/Resources/Localizable.xcstrings
git commit -m "i18n: 补全 zh-Hans 翻译遗漏"
```

---

## Self-Review（写计划后自查记录）

- **Spec 覆盖**：①跟随系统+手动覆盖 → Task 1（`.system` 默认 + apply 写/清）+ Task 3（UI）；②全量本地化 → Task 4；③方案 A（String Catalog + AppleLanguages + relaunch）→ Task 1/2/3；④工程配置（knownRegions + CFBundleLocalizations + xcstrings）→ Task 2；⑤错误处理（relaunch 失败回调、无效值回退 `.system`、缺失 key 回退 en）→ Task 1（relaunch completion、appLanguage get 回退）+ Task 2（xcstrings 缺译自动回退 en 是 Xcode 原生）；⑥测试（手动清单）→ Task 3 Step 4 + Task 4 Step 4；⑦范围外（不做日文/热切换/hook）→ 未引入任何相关代码。✓
- **类型一致**：`AppLanguage` 三 case 及 rawValue（`"system"`/`"zh-Hans"`/`"en"`）在 Task 1 定义，Task 3 的 `LanguagePickerRow` 使用一致；`AppSettings.appLanguage`、`AppLanguage.apply()`、`AppLanguage.relaunch(completion:)` 签名跨任务一致。✓
- **占位符扫描**：所有代码步骤均给完整代码；翻译步骤给完整 key→译文表；无 TBD/TODO。✓
- **文件添加**：依赖 fileSystemSynchronizedRootGroup，Task 1/2/3 新建文件无需改 pbxproj 文件引用（已在 Global Constraints 说明）；仅 Task 2 改 `knownRegions`。✓
